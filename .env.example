# API backend the PWA talks to (same as the Expo mobile app by default).
VITE_API_BASE_URL=https://uat.revise.network/zap-api/v1/api

# Set when hosting the PWA under a sub-path, e.g. "/zap/". Leave "/" for root.
# VITE_BASE_PATH=/
